# Dev/CI environment — đầy đủ cho test + lint + chạy tất cả component cục bộ.
# Dùng: `pip install -r requirements/dev.txt`

-r mcp.txt
-r api.txt
-r loaders.txt
-r agents.txt

# Test
pytest==8.3.4
pytest-cov==6.0.0
pytest-asyncio==0.25.2

# Lint
ruff==0.8.4

# Security scanners (chạy local trước khi push để khớp GitLab CI stage `scan`)
bandit==1.8.0
semgrep==1.103.0

# Python project tooling
build==1.2.2.post1
pip-tools==7.4.1
