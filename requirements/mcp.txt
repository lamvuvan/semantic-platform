# MCP server runtime deps. Tham chiếu pyproject.toml [project.optional-dependencies] mcp.

-r base.txt

# Streamable HTTP transport có sẵn từ FastMCP 2.3.
fastmcp==2.10.6

# Redis cho cache context retrieval + rate-limit.
redis==5.2.1

# httpx cho gọi reranker/LLM nội bộ.
httpx==0.28.1
