# Core deps dùng chung bởi mọi component (mcp, api, loaders, agents).
# Yêu cầu Python 3.12 — xem pyproject.toml [project] requires-python.
#
# Pinning: EXACT version (==). Mọi nâng cấp phải qua MR có review để chạy
# regression đầy đủ. Mirror với pyproject.toml; không tự ý đổi 1 bên.

neo4j==5.27.0
PyYAML==6.0.2
jsonschema==4.23.0
PyJWT[crypto]==2.10.1

# JWT [crypto] kéo theo `cryptography` — pin chặt cho reproducible build.
cryptography==44.0.0
